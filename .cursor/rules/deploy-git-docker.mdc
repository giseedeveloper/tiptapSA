---
description: After code changes — commit, push, VPS pull, Docker rebuild
alwaysApply: true
---

# Deploy workflow (TIPTAP SOUTH AFRICA)

## VPS Details
- **Host:** 139.59.151.111 (DigitalOcean – Cape Town)
- **User:** root
- **Password:** HMF$_h9TMN6XT.x
- **Project path:** /root/TIPTAP
- **Domain:** https://tiptapafrica.co.za

## 1. Local — Git

1. `git status` and `git diff` — confirm only intended files.
2. **Commit** with a clear message (why, not just what).
3. **Push** to `origin main`.
4. Never commit `.env`, tokens, or `auth_info_baileys/`.

## 2. VPS — Docker Deploy

Use sshpass to connect (or read from `~/.codeium/windsurf/mcp_config.json`):

```bash
sshpass -p 'HMF$_h9TMN6XT.x' ssh -o StrictHostKeyChecking=no root@139.59.151.111
```

Deploy sequence on the server:

```bash
cd /root/TIPTAP
git pull origin main

# Rebuild Laravel app & queue containers
docker compose build --no-cache app queue
docker compose up -d

# Clear and cache Laravel
docker exec tiptap_app php artisan config:cache
docker exec tiptap_app php artisan route:cache
docker exec tiptap_app php artisan view:cache

# Verify
docker ps --format '{{.Names}} {{.Status}}'
```

## 3. Or use deploy.sh script

```bash
./deploy.sh
```

## 4. Verify

- Open https://tiptapafrica.co.za in browser
- Check manager dashboard text colors are white on dark background
- Report commit hash, branch, and container status to user.
