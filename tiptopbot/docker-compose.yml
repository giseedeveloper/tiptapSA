services:
  bot:
    build: .
    container_name: tiptop-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      # Cloud API v2: webhook, inbound, and notify share PORT (default 3000).
      - "3000:3000"
